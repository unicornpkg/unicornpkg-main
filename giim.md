# Notes on `giim` and `unicornpkg`

`giim` will search `/lib/giim` for plugins if that folder exists.

`giim` plugins that are added to this repository should be prefixed with `giim-plugin-*` for consistency.
